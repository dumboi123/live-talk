import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "avatar.vercel.sh",
      "randomuser.me",
      "ui-avatars.com",
      "images.unsplash.com",
      "picsum.photos",
    ],
  },
};

export default nextConfig;
